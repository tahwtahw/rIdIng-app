import 'package:flutter/material.dart';

class GroupOutingScreen extends StatelessWidget {
  const GroupOutingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('GroupOutingScreen')),
      body: const SizedBox.shrink(),
    );
  }
}
